{% set DistrcitRecordsArrayLength = DistrcitRecords |length %}
{% set counter = 0 %}



{

"data":[

{% for DistrcitRecord in DistrcitRecords %}

    {%  if counter == DistrcitRecordsArrayLength-1 %}
        {
        "id": "{{ DistrcitRecord.id }}",
        "name": "{{ DistrcitRecord.name}}",
        "governorate_name": "{{ DistrcitRecord.governorate.name}}"

        }
    {%  else %}
        {
        "id": "{{ DistrcitRecord.id }}",
        "name": "{{ DistrcitRecord.name}}",
        "governorate_name": "{{ DistrcitRecord.governorate.name}}"
        },
    {% endif %}

    {% set counter = counter + 1 %}
{% endfor %}

]
}
